import React from 'react';
import ReactDOM from 'react-dom/client';
import RECTLandingPage from './RECTLandingPage.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <RECTLandingPage />
  </React.StrictMode>
);
